# Seedance Cloak 사용 튜토리얼

이 문서는 처음 쓰는 동료도 5분 안에 익힐 수 있도록 만든 단계별 가이드입니다.
앱을 처음 켜면 같은 내용의 **인앱 튜토리얼**이 자동으로 뜹니다. (상단 "튜토리얼" 버튼으로 언제든 다시 보기)

---

## 0. 실행

- 배포받은 폴더에서 **`SeedanceCloak.exe`** 더블클릭.
- 소스로 실행할 경우 `run.bat` 더블클릭(또는 `python run.py`).

처음 실행 시 얼굴 검출 모델과 FFmpeg 가 없으면 자동으로 한 번 내려받습니다(인터넷 필요). 이후에는 오프라인에서도 동작합니다.

---

## 1. 영상 넣기

1. 상단 **영상 첨부** 카드의 큰 영역에 영상 파일을 **드래그&드롭** 하거나, 클릭해서 파일을 고릅니다.
2. 아래에 해상도·fps·길이·프레임 수가 표시됩니다.
   - 길이가 4초 이하이면 `⚠ 4초 이하` 안내가 뜹니다 → **4초 채우기**(5장) 사용을 고려하세요.

지원 형식: MP4 · MOV · AVI · MKV · WEBM · M4V · WMV · FLV

---

## 2. 회피 엔진 고르기 (A · B · C)

**회피 엔진** 카드에서 필요한 엔진을 토글로 켭니다. 여러 개 동시 사용 가능.

- **A. Adversarial Cloak** *(기본 ON, 권장)*
  검출기가 의존하는 엣지/고주파 특징을 다중 스케일 노이즈로 파괴합니다. 사람 눈엔 거의 안 보입니다.
- **B. Frequency Perturb**
  얼굴 **임베딩(매칭)** 을 흩뜨립니다. 특정 인물과의 유사도 매칭을 피하고 싶을 때.
- **C. Semantic Evade**
  그레인 + 경계 소프트닝 + 미세 워프로 **검출 자체**를 방해하는 보조 수단.

### 강도 슬라이더
- **A 강도(eps)**: 2~30. 낮을수록 화질↑(덜 보임), 높을수록 회피율↑. 기본 6 권장.
- **B·C 강도(strength)**: 0.01~0.20. 0.05 부근이 무난합니다.

> 팁: 우선 **A 단독 + 트래킹 ON** 으로 테스트하고, 매칭 회피가 더 필요하면 B 를 추가하세요.

---

## 3. 얼굴 그리드 (신규 기능)

**얼굴 그리드** 카드의 우측 토글을 켜면 상세 옵션이 나타납니다. 얼굴을 자동 인식해 **크기·위치에 맞춰** 격자를 씌웁니다.

| 옵션 | 설명 |
|---|---|
| 가로/세로 칸 수 | 격자의 촘촘함 (1~20) |
| 선 두께 | 1~8px (얼굴 크기에 비례해 자동 보정) |
| 투명도(불투명도) | 0.05~1.0 |
| 영역 여백 | 얼굴 박스보다 얼마나 넓게 덮을지 |
| 교차점 점 크기 | 점 표시를 켰을 때 점 반지름 |
| 색상 | 클릭해서 원하는 색 선택 |
| 모양 | **타원(얼굴형)** 또는 **사각형** |
| 얼굴 기울기 정렬 | 눈 위치 기준으로 격자를 회전 |
| 교차점 점 표시 | 격자 교차점에 점 찍기 |

> 격자는 회피 엔진과 **동시에** 쓸 수 있습니다(예: A + 그리드). 격자만 단독으로도 사용 가능합니다.

---

## 4. 트래킹 & 렌더링 품질

**트래킹 & 품질** 카드:

- **얼굴 트래킹 ON** *(권장)*: 얼굴을 프레임 간 추적하고 위치를 부드럽게 보정합니다. 격자와 노이즈가 **떨리지 않습니다(플리커 제거)**. 끄면 매 프레임 독립 검출이라 다소 흔들릴 수 있습니다.
- **렌더링 품질**:
  - **원본 품질 유지 (권장)** — 육안상 무손실(H.264 CRF14).
  - **고품질 / 표준** — 품질·속도 균형.
  - **저용량 (저화질)** — 파일을 작게, 빠르게.
  - **완전 무손실** — 검증/보관용(용량 매우 큼).
  - **HEVC 고품질** — 같은 품질에 용량↓(H.265, 재생 호환성 주의).

내부적으로 프레임을 **한 번만** 인코딩(raw → FFmpeg)하므로 중간 재압축으로 인한 화질 저하가 없습니다.

---

## 5. 4초 채우기 & 저장

**4초 채우기** 카드:

- 우측 토글을 켜면 **목표 길이**(기본 4초) 슬라이더가 활성화됩니다.
- 원본이 목표 길이보다 짧으면, 영상 뒤에 **검은 화면**을 이어붙여 강제로 목표 길이를 맞춥니다.
  - 예) 2초 영상 + 4초 채우기 → 앞 2초 원본, 뒤 2초 검은 화면 = 총 4초.
  - 오디오는 뒤쪽을 **무음으로 패딩**해 길이를 맞춥니다.
- 원본이 이미 목표 길이 이상이면 아무 것도 하지 않습니다.

**저장 경로**: 자동으로 `원본이름_cloaked.mp4` 가 채워집니다. "찾아보기"로 바꿀 수 있습니다.

마지막으로 하단 **실행** 버튼을 누르면 처리가 시작됩니다.
- 진행률 바와 로그, **라이브 미리보기**가 표시됩니다.
- 처리 중에는 버튼이 **취소**로 바뀝니다.
- 완료되면 "폴더 열기"로 결과를 바로 확인할 수 있습니다.

---

## 자주 묻는 질문

- **Q. 배경도 바뀌나요?** — 아니요. 얼굴 ROI 에만 적용되고 경계는 페더 처리되어 배경 화질은 유지됩니다.
- **Q. 여러 명이 나와도 되나요?** — 네. 다중 얼굴을 각각 추적/처리합니다.
- **Q. 소리가 없어졌어요.** — exe 와 같은 폴더의 `ffmpeg.exe`/`ffprobe.exe` 를 확인하세요. 없으면 무음으로 저장될 수 있습니다.
- **Q. 너무 느려요.** — 품질을 '표준/저용량'으로 낮추고, 엔진은 A 단독을 권장합니다. 큰 해상도일수록 오래 걸립니다.
